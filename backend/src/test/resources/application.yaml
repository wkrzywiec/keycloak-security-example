spring:
  security:
    ignored: "/actuator/**"
    oauth2:
      resourceserver:
        jwt:
          key-value: "secret"
          jwk-set-uri: "http://keycloak"

logging:
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
        security: DEBUG